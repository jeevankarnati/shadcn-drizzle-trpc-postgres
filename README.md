## Starter-Kit with Shadcn, Drizzle, Trpc and Postgres

First, install the dependencies:

```bash
bun install
```

Add database credentials in .env
Generate and push tables to database:

```bash
bun db:generate
bun db:push
```

Now, run the development server:

```bash
bun dev
```
